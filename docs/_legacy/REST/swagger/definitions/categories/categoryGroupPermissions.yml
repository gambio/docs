categoryGroupPermissions:
    title: categoryGroupPermissions
    type: object
    required:
        - id
        - isPermitted
    properties:
        id:
            type: integer
            format: int64
            example: 1
        isPermitted:
            type: boolean
            example: false