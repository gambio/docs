GXOrderStatus:
    title: GXOrderStatus
    type: object
    required:
      - names
      - color
    properties:
        id:
            type: integer
            format: int64
            readOnly: true
        names:
            $ref: '#/definitions/GXMultiLangOption'
        color:
            type: string
    example:
        names:
            en: 'Pending'
            de: 'Offen'
        color: 'f6ae49'
