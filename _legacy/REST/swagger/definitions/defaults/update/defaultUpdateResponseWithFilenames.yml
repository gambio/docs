defaultUpdateResponseWithFilenames:
    title: defaultUpdateResponseWithFilenames
    type: object
    properties:
        code:
            type: integer
            format: int64
        status:
            type: string
        action:
            type: string
        oldFilename:
            type: string
        newFilename:
            type: string
