defaultSuccessResponseWithEmailID:
    title: defaultSuccessResponseWithEmailID
    type: object
    properties:
        code:
            type: integer
            format: int64
        status:
            type: string
        action:
            type: string
        emailId:
            type: integer
            format: int64
