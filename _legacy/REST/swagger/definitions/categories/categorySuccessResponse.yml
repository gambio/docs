categorySuccessResponse:
    title: categorySuccessResponse
    type: object
    properties:
        code:
            type: integer
            format: int64
        status:
            type: string
        action:
            type: string
        resource:
            type: string
        categoryId:
            type: integer
            format: int64
